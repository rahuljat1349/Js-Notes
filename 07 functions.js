


        function sum(a, b) {
            return a + b;
        };

        let v = sum(6, 9);
        console.log(v);



        // or 

        let s = (x, y) => x + y;
        let b = s(5, 5);
        console.log(b);

   